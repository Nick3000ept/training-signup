# Обучение — запись участников

Сайт «Альфа Строй. Работаем с ИИ.» — коллеги записываются на обучение: указывают ФИО и отдел.
Вкладки: Запись / Участники / Программа / Подготовка / Материалы. Дизайн в стиле Apple
(сегментные вкладки, карточки, синие кнопки-«таблетки»).

- **Участники**: сверху карточка-счётчик «Итого участников», ниже все отделы
  из таблицы (пустые тоже, с количеством 0), люди строками внутри отдела.
- **Программа**: структура обучения — 13 занятий (общая база 3 + модули по
  направлениям 5 + сессия вопросов и ответов 1 + трек ядра 4) и карточка «Формат»,
  плюс кнопка на полный PDF `programma.pdf` — копия
  `Разовые_задачи/Обучение ИИ/КП/КП_АС_Орлов_без_стоимости.pdf`.
  ⚠️ Публиковать только версию **без стоимости** (сайт публичный).
  Состав занятий раскрывается по клику (`details.prog`): три занятия общей базы
  и пять модулей А–Д (внутри — подзаголовки «Общие темы», «Разбор на задачах АС»,
  «Разбирается практически», «Обзорно», «Домашнее задание»), а также четыре занятия
  трека ядра. Источник содержания с 2026-09-18 — Приложение № 1 к договору
  (техническое задание: состав, тематика и график), оно же задаёт размеры групп
  (база 35–40, модули 25–30, трек ядра 5–10) и число задач (59 от восьми подразделений).
  ⚠️ `programma.pdf` — версия прежнего КП (12 занятий); при обновлении файла
  заменить его на PDF, собранный из нового техзадания.
- **Подготовка**: разделы из `Материалы/Запрос_материалов_для_окончательного_формирования_программы.pdf`
  (аккордеоны по 10 подразделениям — первыми, затем главное требование,
  минимальный комплект, сроки; блоки «структура папок» и «как подготовить и
  передать файлы» со страницы убраны — структура живёт на Диске, пункты описания
  кейса — в подсказке окна загрузки). Кнопки
  «Приложить документ» — только внутри аккордеонов подразделений, каждая через
  `data-folder` подставляет раздел. Модальное окно: файл (до 20 МБ) + фамилия +
  раздел программы + описание кейса (textarea, до 2000 знаков, под ним —
  подсказка с пунктами описания из документа). Поля «Отдел» в окне нет —
  раздел программы его заменяет (решение пользователя 2026-09-03).
  Файл уходит POST'ом `action=upload` (base64), бэк кладёт его на Диск в
  `Материалы_для_программы_АС/<раздел>` (папки создаются сами; раздел проверяется
  по белому списку `UPLOAD_SUBFOLDERS`: 00_Описание_набора … 10_ИТ_ИБ) и пишет
  строку на лист «Загрузки» (создаётся сам, ищется по A1 = «Дата загрузки»).
- **Материалы**: карточки «текст слева — кнопка справа»: «Вводный урок с Влад NGI.
  05.08.2026» (Яндекс.Диск), «Задачи сотрудников» (antonanton.any.org). Карточка
  с видео Орлова у Комаровского убрана (решение пользователя 2026-09-11). Ниже — «Приложенные документы»: список загрузок с листа
  «Загрузки», сгруппирован по разделам программы (имя файла, описание кейса,
  фамилия, дата); ссылки на Диск на сайт **не отдаются** — файлы приватные.

## Подключения

- **Google-таблица**: ID `1LZOXlwmEaJHBQ8HE59LWpmlgkWGcvRVxvoyIti6BKrg`
  (владелец kuzkin@acons.group).
  - Лист отделов: заголовок A1 = «Подразделение», отделы в колонке A ниже.
  - Лист участников: заголовки «ФИО» и «Отдел» в первой строке.
  - Код находит листы **по заголовкам**, а не по именам — переименование листов не ломает сайт,
    но заголовки первой строки менять нельзя.
- **GAS-скрипт** (standalone, аккаунт kuzkin@acons.group):
  `1BkbaGdCn8l-Aqy7jQrEc-d-jSuWZAq8EVL3HJpx8AtEhiFgHvPS5YtaR`
- **Deployment ID** (не создавать новый, только обновлять!):
  `AKfycbx4OoQXUlWmbVgfQbYkDM_S5YTFRuUuYOKbC03dajF33mIL9QsOCFaLY_GL3cYsTl--`
- **Фронт**: GitHub Pages, репозиторий `Nick3000ept/training-signup`,
  прод-URL https://nick3000ept.github.io/training-signup/

## API

- `GET ?action=ping` → `{ok:true, pong:true}`
- `GET ?action=load` → `{ok, departments:[], participants:[{fio,dept}],
  uploads:[{date,surname,dept,file,desc,folder}]}` (uploads — без ссылок на Диск)
- `POST` (text/plain, JSON `{action:'save', fio, dept}`) → добавляет строку [ФИО, Отдел]
  на лист участников; проверяет отдел по списку, дубликаты (ФИО+отдел без учёта регистра),
  экранирует ввод (`safeCell_`).
- `POST` (text/plain, JSON `{action:'upload', surname, folder, desc, fileName,
  mimeType, data}`, `data` — файл в base64, лимит ~20 МБ) → сохраняет файл на Диск
  в `Материалы_для_программы_АС/<folder>` (folder — из белого списка
  `UPLOAD_SUBFOLDERS`, обязателен), добавляет строку
  [Дата, Фамилия, Отдел, Файл, Ссылка, Описание, Раздел] на лист «Загрузки»
  (колонка «Отдел» историческая, с 2026-09-03 пишется пустой); текстовые поля —
  через `safeCell_` (описание до 2000 знаков). Фронт шлёт без ретраев
  (чтобы не задвоить файл).

## Правила безопасности

- Запись — **только добавление строк**: лист участников (колонки A–B) и лист «Загрузки»
  (колонки A–G, плюс дозаполнение заголовков F1/G1). Лист отделов read-only.
- Строки и листы не удалять.
- Разрешённые файлы: `index.html`, `script.gs`, `appsscript.json`, `CLAUDE.md`,
  `.gitignore`, `.claspignore`, `programma.pdf`.

## Деплой

1. Бэк: `clasp push --force` → `clasp update-deployment <deploymentId> --description "vN: ..."`
   (`.claspignore` пускает в облако только `script.gs` и `appsscript.json`).
2. Фронт: `git push` → GitHub Pages публикует сам (~1 мин, Ctrl+F5).

## Нюансы

- Скрипту нужна была разовая авторизация владельца в редакторе GAS (функция `authorize`) —
  выполнена 2026-09-02. ⚠️ После добавления загрузки файлов (2026-09-03) скрипту нужен
  ещё и доступ к Диску — владельцу надо **повторно** запустить `authorize` в редакторе GAS
  и принять новые права, иначе `action=upload` будет падать с ошибкой авторизации.
- Тестировать POST к бэку через curl — **без** флага `-X POST` (иначе на редиректе Google
  отдаёт ошибку 411, хотя запись при этом проходит; видна ложная ошибка).
- Русский текст в curl из PowerShell передавать файлом (`--data-binary "@file"`, UTF-8 без BOM).
